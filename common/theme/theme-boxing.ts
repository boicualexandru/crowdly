import { StyleSheet } from "react-native";

export const ThemeBoxing = StyleSheet.create({
  container: {
    padding: 16,
    width: "100%",
    flexDirection: "column",
    alignItems: "flex-start",
  }
});